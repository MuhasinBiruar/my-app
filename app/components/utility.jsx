const hover = (scaleNum) => `transition-transform duration-300 ease-in-out hover:scale-${scaleNum}`;

export const divGap = (gapNum) => `flex items-center gap-${gapNum}`;

export default {hover, divGap};

